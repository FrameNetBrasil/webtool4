<form id="{{$id}}" name="{{$id}}" {{$attributes}}>
    <div class="ui card h-full w-full mb-2">
        <div class="flex-grow-1 content bg-white formgroup-inline">
            {{$fields}}
            {{$buttons}}
        </div>
    </div>
</form>
